<%= form_for(@user) do |f| %>

  <%= f.label :nickname %>
  <%= f.text_area :nickname %>

  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :address %>
  <%= f.text_area :address %>
   
  <%= f.submit %>
<% end %>
