<p id="notice"><%= notice %></p>

<h1>users index</h1>

<table>
  <thead>
    <tr>
      <th>User nickname</th>
      <th>User email</th>
      <th>User description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= user.nickname %></td>
        <td><%= user.email %></td>
        <td><%= user.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
